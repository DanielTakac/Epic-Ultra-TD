using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bird1 : MonoBehaviour{

    /*private bool forward = true;
    private bool right = false;
    private bool left = false;

    public float speed = 10f;

    public float rotateSpeed = 10f;

    void Update(){

        if (Input.GetKey("i")) { forward = false ; } else { forward = true; }
        if (Input.GetKey("o")) { left = true; } else { left = false; }
        if (Input.GetKey("p")) { right = true; } else { right = false; }

        if (forward) { Forward(); }
        if (right) { Right(); }
        if (left) { Left(); }
        
    }

    private void Forward(){

        transform.position += transform.forward * speed * Time.deltaTime;

    }

    private void Right(){

        transform.Rotate(new Vector3(0f, rotateSpeed * Time.deltaTime, 0f));

    }

    private void Left(){

        transform.Rotate(new Vector3(0f, -rotateSpeed * Time.deltaTime, 0f));

    }*/

}
